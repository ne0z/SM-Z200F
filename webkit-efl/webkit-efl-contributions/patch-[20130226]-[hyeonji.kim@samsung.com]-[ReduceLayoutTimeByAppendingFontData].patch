diff --git a/Source/WebCore/ChangeLog b/Source/WebCore/ChangeLog
index 743984a..6603d20 100644
--- a/Source/WebCore/ChangeLog
+++ b/Source/WebCore/ChangeLog
@@ -1,3 +1,21 @@
+2013-02-25  Hyeonji Kim  <hyeonji.kim@samsung.com>
+
+        Add system fallback FontData to FontFallbackList
+        https://bugs.webkit.org/show_bug.cgi?id=110838
+
+        Reviewed by NOBODY (OOPS!).
+
+        Add system fallback FontData to FontFallbackList to reduce layout time.
+        When system fallback font's glyph page dosen't have the character, especially CJK,
+        Page layout takes a long time due to create a fontData for the character.
+        Add the fontData which has already been created to FontFallbackList
+        not to try to create the same FontData.
+
+        No new tests, covered by existing tests.
+
+        * platform/graphics/FontFastPath.cpp:
+        (WebCore::Font::glyphDataAndPageForCharacter):
+
 2013-02-25  Ilya Tikhonovsky  <loislo@chromium.org>
 
         Unreviewed fix of type annotation for this._liveLocations.
diff --git a/Source/WebCore/platform/graphics/FontFastPath.cpp b/Source/WebCore/platform/graphics/FontFastPath.cpp
index 9a945ca..acb74b0 100644
--- a/Source/WebCore/platform/graphics/FontFastPath.cpp
+++ b/Source/WebCore/platform/graphics/FontFastPath.cpp
@@ -246,6 +246,8 @@ std::pair<GlyphData, GlyphPage*> Font::glyphDataAndPageForCharacter(UChar32 c, b
             characterFontData = characterFontData->variantFontData(m_fontDescription, variant);
     }
     if (characterFontData) {
+        // Add the fallback FontPlatformData
+        m_fontList->setPlatformFont(characterFontData->platformData());
         // Got the fallback glyph and font.
         GlyphPage* fallbackPage = GlyphPageTreeNode::getRootChild(characterFontData.get(), pageNumber)->page();
         GlyphData data = fallbackPage && fallbackPage->fontDataForCharacter(c) ? fallbackPage->glyphDataForCharacter(c) : characterFontData->missingGlyphData();
